rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all access (like before)
    // WARNING: This is not secure for production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}